{
  "name": "trade-learn",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend-tradelearn run dev",
    "build": "npm --prefix frontend-tradelearn run build",
    "preview": "npm --prefix frontend-tradelearn run preview",
    "dev:ws": "npm --prefix backend-tradelearn run dev"
  }
}
