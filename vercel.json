{
  "version": 2,
  "installCommand": "npm --prefix frontend-tradelearn ci",
  "buildCommand": "npm --prefix frontend-tradelearn run build",
  "outputDirectory": "frontend-tradelearn/dist/client",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/api/ssr" }
  ]
}
